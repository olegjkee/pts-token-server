{
  "name": "pts-token-server",
  "version": "1.0.0",
  "description": "PTS Project Token Server - Dynamic token display with admin panel",
  "main": "server.js",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["token", "crypto", "server", "api"],
  "author": "PTS Project",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
